import { styled } from "@mui/material";

const Page = styled("div")({
  display: "flex",
  flexDirection: "column",
  minHeight: "100vh",
  overflow: "hidden",
  width: "100%",
});

export default Page;
